<% housings.each do |housing| %>
  <%= render partial: "housings/#{housing.card_partial_name}", locals: { housing: housing, trip: trip, authorization: housing_authorization(housing) } %>
<% end %>

<% if housings.size.odd? %>
  <div class="housing-card housing-card--empty"><!-- empty one --></div>
<% end %>
